# Do not put Gmail OAuth credentials here.
# This app expects local Gmail OAuth files in the project root:
# - credentials.json
# - token.json
#
# Streamlit Cloud should run Demo Mode only unless web OAuth is added later.
